# Loaded automatically by Vite for production builds (`vite build` / `tauri build`),
# NOT for `npm run dev`. This pins every release (local macOS staple build AND CI)
# at the live backend. No secret here — just the public backend URL.
VITE_CU_BACKEND_URL=https://ai-screen-buddy-backend.pageapp.net
